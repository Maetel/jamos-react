import Window from "../components/Window";

export default function AppStore(props) {
  return (
    <Window {...props}>
      <div className="container">AppStore placeholder</div>
    </Window>
  );
}
