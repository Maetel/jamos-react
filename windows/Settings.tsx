import Window from "../components/Window";

export default function Settings(props) {
  return (
    <Window {...props}>
      <div className="container">Settings placeholder</div>
    </Window>
  );
}
