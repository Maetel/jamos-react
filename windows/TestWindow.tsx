import Window from "../components/Window";

export default function TestWindow(props) {
  return (
    <Window>
      <div className="test-window">This is a test window content.</div>
    </Window>
  );
}
